import styled from "styled-components";

const Body = styled.div`
  margin-top: 50px;
  margin-bottom: 75px;
  display: flex;
  flex-direction: row;
`;

const Flex = styled.div`
  display: flex;
  flex-wrap: wrap;
`;

const Logo = styled.img`
  height: 100%;
  width: 100%;
`;

export { Body, Flex, Logo };
